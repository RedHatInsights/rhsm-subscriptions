---
platform: RHEL

id: satellite-server

fingerprint:
  engineeringIds:
    - 250 # Red Hat Satellite

variants:
  - tag: Satellite Server

defaults:
  variant: Satellite Server

metrics:
  - id: SOCKETS
