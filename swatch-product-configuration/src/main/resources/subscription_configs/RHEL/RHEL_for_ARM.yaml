---
platform: RHEL

id: RHEL for ARM

fingerprint:
  engineeringIds:
    - 419 # Red Hat Enterprise Linux for ARM 64
  arches:
    - aarch64
    - arm64

variants:
  - tag: RHEL for ARM

defaults:
  variant: RHEL for ARM

metrics:
  - id: SOCKETS
