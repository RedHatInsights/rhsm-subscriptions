resteasy:
  jaxrs:
    app:
      registration: property
      classes: org.candlepin.insights.JaxrsApplication
management:
  endpoints:
    web:
      exposure:
        include:
          - health
          - shutdown
          - prometheus
  endpoint:
    shutdown:
      enabled: true
    prometheus:
      enabled: true
