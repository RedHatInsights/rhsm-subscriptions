$schema: http://json-schema.org/draft-07/schema#
title: CleanUpEvent
extends:
  $ref: base_event.yaml
properties:
  start:
    description: Start time range used to query events to clean up.
    type: string
    format: date-time
    required: true
  end:
    description: End time range used to query events to clean up.
    type: string
    format: date-time
    required: true