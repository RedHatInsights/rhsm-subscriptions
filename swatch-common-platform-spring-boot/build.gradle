plugins {
    id 'java-platform'
}

dependencies {
    constraints {
        runtime libraries["netty-codec-http2"]
        runtime libraries["commons-io"]
    }
}

description = 'Dependency overrides for spring-boot based swatch components'
